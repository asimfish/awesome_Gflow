# 从这里开始：该不该用 GFlowNet

这份仓库有 213 篇论文、35 篇深度解读、4 份趋势报告。但在读任何一篇之前，先花五分钟回答一个问题：**你的任务适合 GFlowNet 吗？**

领域内的论文几乎都以「我们提出 X 改进了 Y」的框架写作，失效证据埋在实验章节与 limitations 里。结果是读完 200 篇也拼不出「该不该用」的答案。这一页把答案前置。

## 第一步：三个问题决定要不要继续

**问题 1：你需要多个不同的好解，还是一个最优解？**

- 需要一批**结构多样**的高分候选（药物发现的候选池、多样推理路径）→ GFlowNet 的主场，继续。
- 只要单个最优解 → 用 PPO/GRPO 等奖励最大化方法。GFlowNet 的比例采样在这里是纯开销。

GFlowNet 的目标是让终止对象的采样概率满足 $P_\top(x) \propto R(x)$，概率质量按比例铺开在**所有**高奖励模态上。这是它与奖励最大化方法的结构性差异，不是调参能弥合的。

**问题 2：你的奖励地形是什么形状？**

- 奖励峰值**尖锐（近 Dirac）**、维度不高 → **不要用 GFlowNet**。实测数据：8D HyperGrid 上峰宽 $\sigma \le 0.5$ 时 GFlowNet 只找到 1-2 个模态，而普通 Metropolis-Hastings 找到 11-16 个，且快约 500 倍（N081）。GFlowNet 依赖连续可微的学习信号，梯度稀疏时直接崩塌。
- 峰宽 $\sigma \approx 1.0$ 是分水岭；$\sigma = 2.0$ 的宽峰多模态情形 GFlowNet-DB 可多发现约 4.75 倍模态，但这是用约 500 倍算力换的。
- 想用 GFlowNet 就先做奖励平滑（温度退火、log 变换）把 $\sigma$ 推到 1.0 以上。

**问题 3：你有力场、梯度或能量结构可用吗？**

- 有（分子构象、Boltzmann 采样这类物理场景）→ 优先考虑 iDEM（N031）、力引导漂移（N107）这类利用能量结构的方法，它们在物理基准上已是 SOTA。GFlowNet 的强项是**离散组合空间 + 只有终端奖励**，这里用不上。
- 没有，只有一个黑箱打分函数 → GFlowNet 的典型场景。

## 第二步：确认要用之后，避开三个坑

**坑 1：奖励函数不完美时会怎样？** —— 这恰恰是 GFlowNet 的护城河。

治疗性肽实验（N109）：与带显式多样性惩罚的 GRPO-D 对比，GFlowNet 二肽采样均匀度高 5.4 倍、重复序列少 3.9 倍。压力测试更关键——去掉奖励里的熵门控后，GRPO-D 的 1000 个样本 100% 塌成同一个三肽模式，GFlowNet 多样性保持 0.937。真实流水线里奖励超参通常调不准，这条差异比峰值性能重要。

**坑 2：你打算用什么指标证明「学对了分布」？**

这是最容易翻车的地方。以下四个流行指标**都不测分布正确性**（T32 系统性否证）：

| 指标 | 失效方式 |
|---|---|
| $\log p_\theta$ 与 $\log R$ 的 Spearman/Pearson 相关 | $\log p_\theta = c\log R$ 时相关恒为 1.0，但只有 $c=1$ 才匹配目标 |
| 平均未归一化目标 | 对「在模态上堆积过多质量」的错误分布给高分 |
| Shen's accuracy | 同上 |
| 发现的模态数 / top-$k$ 平均奖励 | 只反映搜索能力，与分布正确性无关 |

唯一被验证可用的是 **FCS**（Flow Consistency in Sub-graphs）：与 TV 距离的 Spearman 相关 0.99（集合生成）/ 0.90（序列生成），计算量少约 3 个数量级，有 PAC 界。用 FCS 复检发现 LED- 与 FL-GFlowNets **根本学不到目标分布**，尽管它们用模态数评测会被判为优胜。

**操作规则**：自己的实验报 FCS 或（可枚举时）精确 TV；读别人论文只看到模态数与 top-$k$ 奖励，就假定分布正确性未被验证。

**坑 3：训练不稳定是正常的还是出了问题？**

- **loss 尖峰不一定是问题**：T51 构造了反例——小 TV 误差不蕴含有界训练 loss，分布已经全局准确时 loss 仍可能剧烈波动。别看到尖峰就调参。
- **系统性欠拟合是普遍现象**：GFlowNet 以过高概率采样低奖励对象，$\log p_\theta$ 对 $\log R$ 的回归斜率在原始论文的小分子任务上只有 0.58（理想 1.0），数万轮后仍达不到目标均值（T07、T01）。
- **稀疏奖励下要检查轨迹截断**：人为的长度上限产生「强制终止态」，违反流守恒边界约束造成流泄漏，把生成偏向最长轨迹并触发 mode collapse。Pruning Loss（N113）修这个问题——但只在稀疏奖励下有收益，稠密奖励下各方法相当。

## 第三步：选训练目标

别照搬理论收敛速率。T40 给出 FM $\mathcal{O}(1/\sqrt{T})$ 优于 DB $\mathcal{O}(1/T^{1/3})$，但那是 tabular 假设下的结果；N081 在高维稀疏离散图上实测 **FM 多数情况不收敛、DB 一致最优**，排序相反。

实操建议：

| 情形 | 起点目标 | 依据 |
|---|---|---|
| 短轨迹、需要熵覆盖 | FM | T40 证明 FM 偏最大熵解 |
| 长轨迹、要快收敛 | TB（配 reference flow 类稳定器） | T03 缩短信用路径；T51 提供稳定化 |
| 高维稀疏离散图 | DB | N081 实测 |
| 想在 DB 与 TB 之间调 | SubTB（$\lambda$ 控制尺度） | T05；注意它可能带来有效性退化（T54 实测 Acc 掉到 0.328） |
| LLM 后训练 | TB 系 + 前缀信用分配（RapTB） | T54 命名并修复前缀坍缩、长度偏置 |

## 第四步：按方向读论文

到这一步再进入阅读路线：

- **完整分类目录**（213 篇，按理论/交叉/应用/生态分区）→ [README.md](README.md)
- **按读者类型的阅读顺序**（新手 / 理论 / 应用 / 训练稳定性支线 / OT 支线）→ [资源目录 §0.2](surveys/GFLOWNET_RESEARCH_RESOURCE_CATALOG_CN.md#02-按读者类型选路线)
- **35 篇核心论文的中文深度解读** → [notes/](notes/)
- **中英文对照 PDF** → [pdfs/en/](pdfs/en/) 与 [pdfs/zh/](pdfs/zh/)

四份趋势报告分别回答不同问题：

| 报告 | 回答的问题 |
|---|---|
| [方法学前沿](insights/trends_methods.md) | 2026 年该往哪个方向做？哪条路已经走满？ |
| [邻域竞品](insights/trends_neighbors.md) | GFlowNet 相对其他方法的位置是上升还是下降？ |
| [应用与生态](insights/trends_applications.md) | 工业界真的在用吗？哪个库能依赖？ |
| [失效边界](insights/trends_failure_modes.md) | 什么时候它不 work？（本页是它的精简版） |

## 一句话总结

GFlowNet 值得用的场景是：**离散组合空间 + 需要多样高分候选 + 奖励函数难以精调 + 奖励地形不是尖峰**。四个条件都满足时它的护城河很清晰；缺一个就该认真考虑替代方案。

---

*所有数字都有出处，编号对应本仓库目录（`T`/`O`/`A`/`N` 系列）。产物的生成方式与已知局限见 [DELIVERY.md](DELIVERY.md)。*
